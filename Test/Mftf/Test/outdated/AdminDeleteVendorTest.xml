<?xml version="1.0" encoding="UTF-8"?>

<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="../../../../../../vendor/magento/magento2-functional-testing-framework/src/Magento/FunctionalTestingFramework/Test/etc/testSchema.xsd">
    <test name="AdminEraseVendor">
        <annotations>
            <features value="Vendor Delete"/>
            <stories value="Delete a Vendor via the Admin"/>
            <title value="You should be able to delete a Vendor in the admin back-end."/>
            <description value="You should be able to delete a Vendor in the admin back-end."/>
            <group value="mageinn"/>
            <group value="vendor"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdminForVendor1"/>
        </before>

        <actionGroup ref="SelectCreatedVendors" stepKey="selectCreatedVendors"/>
        <!--Delete the vendor-->
        <click selector="{{AdminVendorFormActionsSection.DeleteVendor}}" stepKey="deleteVendor"/>
        <click selector="{{AdminVendorFormActionsSection.VendorDeleteConfirmOK}}" stepKey="confirmDelete"/>
        <seeElement selector="{{AdminVendorMessagesSection.SuccessMessage}}" stepKey="assertDeleteSuccess"/>
    </test>

    <test name="AdminEraseVendorCancelOnPopUp">
        <annotations>
            <features value="Vendor Delete"/>
            <stories value="Delete a Vendor with cancel on popup"/>
            <title value="Delete a vendor and canceling the deletion"/>
            <description value="Delete a vendor and canceling the deletion"/>
            <group value="mageinn"/>
            <group value="vendor"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdminForVendor1"/>
        </before>

        <actionGroup ref="SelectCreatedVendors" stepKey="selectCreatedVendors"/>
        <!--Delete the vendor-->
        <click selector="{{AdminVendorFormActionsSection.DeleteVendor}}" stepKey="deleteVendor"/>
        <click selector="{{AdminVendorFormActionsSection.VendorDeleteConfirmCancel}}" stepKey="cancelDelete"/>
        <seeElement selector="{{AdminVendorFormFieldsTitles.VendorTitle}}" stepKey="checkVendorTitle"/>
    </test>
</tests>